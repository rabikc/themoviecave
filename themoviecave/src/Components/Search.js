import React, { useEffect, useState } from 'react';
import axios from 'axios';

const Search = () => {

  // const [searchText, setSearchText] = useState();

  // const search = async () => {
  //   try{
  //     const  { data } = await axios.get(
  //       `https://api.themoviedb.org/3/search/company?api_key=${process.env.REACT_APP_API_KEY}&language=en-US
  //       &query=${searchText}&sort_by=popularity.desc&include_adult=false&include_video=false&page=${page}&with_genres=${genreforURL}`
  //     )
  //     setContent(data.results);
  //     setNumOfPages(data.total_pages)
  //   }
  //   catch(error) {
  //     console.log(error)
  //   }
  // }

  return (
    <></>
  )
}

export default Search